// Define your favorite numbers
const firstFavNumb = 7;  // Replace with your favorite number
const secondFavNumb = 10; // Replace with your second favorite number

// Perform and log various comparisons

// Greater than
console.log(`${firstFavNumb} > ${secondFavNumb}:`, firstFavNumb > secondFavNumb);

// Less than
console.log(`${firstFavNumb} < ${secondFavNumb}:`, firstFavNumb < secondFavNumb);

// Greater than or equal to
console.log(`${firstFavNumb} >= ${secondFavNumb}:`, firstFavNumb >= secondFavNumb);

// Less than or equal to
console.log(`${firstFavNumb} <= ${secondFavNumb}:`, firstFavNumb <= secondFavNumb);

// Strict equality (===)
console.log(`${firstFavNumb} === ${secondFavNumb}:`, firstFavNumb === secondFavNumb);

// Loose equality (==)
console.log(`${firstFavNumb} == ${secondFavNumb}:`, firstFavNumb == secondFavNumb);

// Strict inequality (!==)
console.log(`${firstFavNumb} !== ${secondFavNumb}:`, firstFavNumb !== secondFavNumb);

// Loose inequality (!=)
console.log(`${firstFavNumb} != ${secondFavNumb}:`, firstFavNumb != secondFavNumb);

// AND (&&)
console.log(`(${firstFavNumb} > 0 && ${secondFavNumb} > 5):`, firstFavNumb > 0 && secondFavNumb > 5);

// OR (||)
console.log(`(${firstFavNumb} > 10 || ${secondFavNumb} > 5):`, firstFavNumb > 10 || secondFavNumb > 5);

// NOT (!)
console.log(`!(${firstFavNumb} === ${secondFavNumb}):`, !(firstFavNumb === secondFavNumb));
